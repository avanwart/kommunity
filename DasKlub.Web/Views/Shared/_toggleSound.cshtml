@using DasKlub.Lib.Resources

<script type="text/javascript">

    function toggleSound() {
        if (player != null) {
            // BUG: YOUTUBE API SAYS THESE AREN'T FUNCTIONS
            if (player.getVolume() > 0) {
                player.setVolume(0);
                $("#speaker_sound").attr("src", $rootUrl + "content/images/speaker_off.png");
                $("#speaker_sound").attr("alt", "off");
                $("#speaker_sound").attr("title", '@Html.Raw(Messages.TurnSoundOn)');
                $("#speaker_words").text('@Html.Raw(Messages.SoundOff)');
            } else {
                player.setVolume(level);
                $("#speaker_sound").attr("src", $rootUrl + "content/images/icons/speaker_on.png");
                $("#speaker_sound").attr("alt", "on");
                $("#speaker_sound").attr("title", '@Html.Raw(Messages.TurnSoundOff)');
                $("#speaker_words").text('@Html.Raw(Messages.SoundOn)');
            }

        }
    }

</script>