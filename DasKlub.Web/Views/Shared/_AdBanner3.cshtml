@using DasKlub.Lib.AppSpec.DasKlub.BOL.DomainConnection
@using DasKlub.Lib.BOL.DomainConnection
@using DasKlub.Lib.Operational
@using DasKlub.Lib.Values

<!-- 120 x 60 -->

 
 

@{
    int randomID = Utilities.RandomNumber(1, 4);

    switch (randomID)
    {
        case 1:
            @Html.Raw(SiteDomain.GetSiteDomainValue(
                SiteEnums.SiteBrandType.A1201,
                Utilities.GetCurrentLanguageCode()))
            break;
        case 2:
            @Html.Raw(SiteDomain.GetSiteDomainValue(
                SiteEnums.SiteBrandType.A1202,
                Utilities.GetCurrentLanguageCode()))
            break;
        default:
            @Html.Raw(SiteDomain.GetSiteDomainValue(
                SiteEnums.SiteBrandType.A1203,
                Utilities.GetCurrentLanguageCode()))
            break;
    }
}